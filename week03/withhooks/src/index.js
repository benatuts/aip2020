import React from 'react';
import ReactDOM from 'react-dom';
import Selector from './Selector';

// Start React in the #root element
ReactDOM.render(
    <React.StrictMode>
        <Selector/>
    </React.StrictMode>,
    document.getElementById('root')
);
